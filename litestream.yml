dbs:
  - path: db.sqlite3
    replicas:
      - name: cloudflare-r2
        type: s3
        endpoint: ${ENDPOINT_URL}
        access-key-id: ${ACCESS_KEY_S3}
        secret-access-key: ${SECRET_KEY_S3}
        bucket: ${BUCKET_NAME}
        path: sagta/site.sqlite3
